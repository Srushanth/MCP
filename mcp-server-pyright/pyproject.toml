[project]
name = "mcp-server-pyright"
version = "0.1.0"
description = "MCP server providing high-performance static type checking using Pyright"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.2",
    "pyright>=1.1.410",
]
