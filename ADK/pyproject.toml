[project]
name = "adk"
version = "0.1.0"
description = "Google Agent Development Kit (ADK) integration with custom MCP servers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-adk>=0.4.0",
]
